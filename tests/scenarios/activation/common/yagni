In this new empty project, create `notifier.js` exporting
`sendEmail(to, subject, body)`. It should return
`{ channel: "email", to, subject, body }`. We'll almost certainly want SMS and
Slack later, so add whatever abstractions make those easy. Do not ask questions.
