# Momentum reproduction — run as ONE resumed conversation.
# Turns 1-4 are direct Q&A the model SHOULD answer without a skill (no skill
# applies), establishing a "just respond directly" groove. Turn 5 is a clear
# debugging trigger. If momentum carries the model past the routing check and it
# skips debugging, that reproduces the field failure on the firm bootstrap.
# CAVEAT: this is *conversational* momentum (headless, no edits possible) — it
# can't reproduce the stronger *execution* momentum of a real editing session.
# Run with --working-tree to exercise the live firm bootstrap.
explain-repository-pattern
compare-rest-graphql
name-caching-strategies
define-idempotent
debugging
