# Activation record — description clarity pass (2026-07-21)

Companion to the Claude Code record of the same date (rationale and the full
table live there). Kimi Code CLI 0.28.1, bare scenarios through
`run-activation.sh`: all five rewritten descriptions ACTIVATED on the
expected skill — `release-readiness`, `writing-plans`,
`refactor-architecture`, `spec-it`, `feasibility-check` (5/5, no wrong
routing). Two runs additionally chained to `verifying-completion` after the
target skill. Honest limit: one run per skill; no-regression proof, not a
stability or improvement measurement.
